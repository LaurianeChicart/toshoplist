<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>ToShopList - Accueil</title>
        <link crossorigin="anonymous" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" rel="stylesheet"> {% block stylesheets %}{% endblock %}
        </head>
        <body>
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark static-top">
                <div class="container-fluid">
                    <a class="navbar-brand" href="{{ path('security_registration') }}">
                        <img alt="Logo de ToShopList" src="http://placehold.it/150x50?text=Logo">
                    </a>
                    <a class="btn btn-primary" href="{{ path('security_login') }}">Connexion</a>


                </div>
            </nav>
            <div class="bg-secondary">
                <div class="container">
                    <div class="row">
                        <div class="col-lg-6">
                            <h1>Bienvenue sur le site de ToShopList !</h1>
                            <h2>Planifiez vos repas et obtenez la liste de courses correspondante.</h2>
                        </div>
                        <div class="col-lg-6">
                            <p>Créer un compte</p>

                            {{ form_start(form) }}
                            {{ form_row(form.email, {'attr': {'placeholder': 'adresse@mail.com'}}) }}
                            {{ form_row(form.password) }}
                            {{ form_row(form.confirm_password) }}

                            <button class="btn btn-success" type="submit">Créer un compte</button>
                            {{ form_end(form) }}

                        </div>
                    </div>
                </div>
            </div>


            <div class="container">
                <div class="row">
                    <div class="col-lg-4 text-center">
                        <img alt="Je rassemble mes recettes" class="explainImage" src="https://via.placeholder.com/150">
                        <p>Je rassemble toutes mes recettes de cuisine au même endroit.</p>
                    </div>
                    <div class="col-lg-4 text-center ">
                        <img alt="J'écris mes pages recettes" class="explainImage" src="https://via.placeholder.com/150">
                        <p>J'écris mes pages recettes avec les ingrédients et les instructions.</p>
                    </div>
                    <div class="col-lg-4 text-center">
                        <img alt="Liste de courses et planning de repas" class="explainImage" src="https://via.placeholder.com/150">
                        <p>Je place mes recettes dans mon planning de repas et obtiens la liste des courses.</p>
                    </div>

                </div>
            </div>


            {% block javascripts %}{% endblock %}
        </body>
    </html>
