<html>
    <head>
        <meta charset="utf-8">
        <meta content="width=device-width, initial-scale=1, shrink-to-fit=no" name="viewport">
        <link href="{{ asset('css/bootstrap/compiled/bootstrap.min.css') }}" rel="stylesheet">
    </head>
    <body>
        <div class="container mb-3 mt-3">
            <h2 class="text-secondary">ToShopList</h2><br>
            <p>Email de :
                {{ email }}</p>
            <p>Objet :
                {{ subject }}</p>
            <p>{{ message }}</p>
        </div>
    </body>
</html>
